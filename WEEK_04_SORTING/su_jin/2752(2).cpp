/*
#include<iostream>
#include<algorithm> //sort함수가 포함된 헤더파일

using namespace std;
int main() {
	int a[3];
	int input;
	
	for (int i = 0; i < 3; i++) {
		cin >> input;
		a[i] = input;
	}
	sort(a, a + 3); 
	//sor함수(시작지점,끝지점), 시작점은 포함 끝지점은 포함X, 인덱스 0~2까지
	
	for (int i = 0; i < 3; i++) {
		cout << a[i];
	}

}
*/